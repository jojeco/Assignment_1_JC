# Next Increment Suggestions

- Add `accessibilityLabel` props to all `TextInput` and `Pressable` components so screen readers can identify each field and button.
- Migrate the component to TypeScript (`.tsx`) and add basic prop types / interfaces — good practice for a CS portfolio piece.
- Add a `README.md` with setup instructions (`npx expo start`), a brief description of the app, and a screenshot so the repo is presentable on GitHub.
- Add input validation: disable the "Make my Hall Pass!" button (or show an inline warning) when any field is empty, preventing the hall-pass page from rendering with blank values.
